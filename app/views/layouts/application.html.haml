!!!
%html
  %head
    %title
      = content_for :title
      \- cloudscontrol.us
    %meta{:name => 'Description', :content => 'cloudscontrol.us is a private community of artists, musicians, and other creative professionals who need a place to store and trade assets, share new work, and communicate with others.'}
    %meta{:name => 'Keywords', :content => 'cloudscontrol.us, clouds control us, clouds, collaboration, file collaboration, art collaboration, art display, music collaboration, music'}

    = stylesheet_link_tag 'control'
    = csrf_meta_tag
    = yield(:head)

  %body
    #container
      #banr
        cloudscontrol.us

      #navbar
        %ul
          %li= link_to 'Home', root_path
          %li= link_to 'Users', users_path
        %p
          - if user_signed_in?
            - unless current_user.username.blank?
              = link_to current_user.to_param, user_path(current_user)
            - else
              = current_user.email
            \|
            = link_to 'Profile', edit_user_registration_path
            \|
            = link_to 'Sign Out', destroy_user_session_path
          - else
            = link_to 'Register', new_user_registration_path
            \|
            = link_to 'Sign In', new_user_session_path

      - flash.each do |name, msg|
        - if msg.is_a?(String)
          = content_tag :div, msg, :class => 'flash', :id => "flash_#{name}"

      #content
        = yield

      #foot
        © 2010 cloudscontrol.us

    = javascript_include_tag :defaults
    :javascript
      var _gaq = _gaq || [];
      _gaq.push(['_setAccount', 'UA-19148581-1']);
      _gaq.push(['_trackPageview']);

      (function() {
        var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
        ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
        var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
      })();
