- content_for :title, 'Create an account'
- content_for :box, render(:partial => 'users/help')

%h1= content_for :title

%p Want in? It all starts here.

%p Enter your email address and a new password to create an account. After you've confirmed your email address, you'll be able to log in, create a profile, and participate in the cloud control experience.

= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  = devise_error_messages!

  %p
    %b= f.label :email
    %br/
    = f.text_field :email

  %p
    %b= f.label :password
    %br/
    = f.password_field :password

  %p
    %b= f.label :password_confirmation
    %br/
    = f.password_field :password_confirmation

  %p= f.submit 'Sign up'

%p
  = link_to 'Forgot your password?', new_password_path(resource_name)
  %br/
  = link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name)
