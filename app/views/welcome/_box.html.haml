%h3 Recent Posts

%ul
  - Post.top.first(5).each do |post|
    %li
      = link_to post.subject, user_post_path(post.user, post)
      - unless @user == post.user
        by
        = link_to post.user.to_param, user_path(post.user)

