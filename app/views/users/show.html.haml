- content_for :title, @user.to_param

#news
  = render :partial => 'users/info'

  %p= link_to 'Posts', user_posts_path(@user)

- if @user.content.blank?
  %h1 Profile

  This user has not yet created any content.

- else
  :textile
    #{@user.content}
